{
    "name": "flappy-bird",
    "workspaces": [
        "workspaces/server",
        "workspaces/client"
    ],
    "scripts": {
        "dev": "concurrently \"npm run dev --workspace=server\" \"npm run dev --workspace=client\""
    },
    "devDependencies": {
        "concurrently": "^8.2.2",
        "cross-env": "^7.0.3",
        "env-cmd": "^10.1.0"
    }
}
